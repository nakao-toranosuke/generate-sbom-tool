#!/usr/bin/env sh
set -eu

python --version
python -m pip show streamlit
streamlit version
node --version
npm --version
java -version
mvn --version
php --version
composer --version
ruby --version
gem --version
bundle --version
rustc --version
cargo --version
go version
dotnet --version
trivy --version
python -m pip check
